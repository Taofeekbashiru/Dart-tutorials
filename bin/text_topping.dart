void main(List<String> args) {
  // const text = "I like pizza";
  // const topping = "with tomato";
  // var favourite = '$text, $topping';
  // final newtext = favourite.replaceAll('pizza', 'pasta');
  // favourite = 'Now i like curry';
  // print(newtext);
  // final list = [1, 2, 3];
  // var copy1 = list;
  // copy1[0] = 0;
  // final copy2 = [...list];
  // copy2[2] = 7;
  // print(list);
  // print(copy1);
  // print(copy2);
  var odds = [1, 3, 5, 7, 9];
  var sum = 0;
  for (var item in odds) {
    sum += item;
  }
  print(sum);
}
